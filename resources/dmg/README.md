# DMG installation background

- `install-guide-bilingual-source.png` is the original 1611 × 976 bilingual artwork.
- `install-guide-bilingual.png` is the 660 × 400 production background used by Tauri.

The DMG places the real application icon at `(143, 160)` and the Applications
folder alias at `(516, 160)`, aligned to the two illustrated landing areas.
